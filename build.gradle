apply plugin: 'java'
apply from: 'ext.gradle'

allprojects {
    group = 'ro.szzsa.gallery'
    version = '1.0.0-SNAPSHOT'

    repositories {
        mavenCentral()
        jcenter()
    }

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}